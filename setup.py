from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='It is the basic project creation with all required files in git',
    author='Pradeep V',
    license='MIT',
)
